# SpecDSL 構文ガイドライン (Syntactic Guide)

このドキュメントは、`SpecDslParser` が解析する独自DSL（Domain Specific Language）の構文をまとめたものです。本DSLは、クラス図や振る舞い（Gherkinステップ）からシステムモデルを抽出するために使用されます。

## 1. 基本構造とブロック識別子

### A. ファイルヘッダ (Header Block)
ファイル先頭に記述されたコメント (`//` または `#`) や `alias` 宣言は、本体のパース対象外ですが、DSL全体の設定情報や説明として保持されます。

### B. Alias 宣言
型名やドメインワードの別名を定義します。
**構文:**
```dsl
alias "日本語表記" as "英語識別子"
```
*   **`"日本語表記"`**: DSL内で使用される人間が理解しやすいキーワード（例: `受注金額`, `ユーザーID`）。
*   **`as`**: キーワード。
*   **`"英語識別子"`**: 実際のコードやシステムで使われるべき、単語の組み合わせ（例: `orderAmount`, `userId`）。

---

## 2. クラス定義 (Class Definition)

クラスは構造体、インターフェース、抽象クラスを定義します。

### 基本構文
```dsl
[abstract] class/interface/struct <ClassName> [extends <BaseName>] [implements <Interface1>, <Interface2>, ...] {
    // メンバや操作の宣言が続く
}
```

*   **`[abstract]`**: クラスを抽象クラスにする場合は `abstract` キーワードを先頭につけます。
*   **`class`, `interface`, `struct`**: 定義する型種別を指定します。（例: `class User {}`）
*   **`extends <BaseName>`**: 継承元（親クラスまたはインターフェース）を指定します。
*   **`implements <Interface1>, ...`**: 実装するインターフェース名をカンマ区切りで指定します。

### クラスレベルの追加設定 (Needs)
メンバや操作宣言ブロック内の特定の行に、その要素が持つべき付帯情報（例: 所有者、理由）を指定できます。これは直接的な構文というより、**直後のメンバー/操作定義に対するメタデータ付与**として機能します。

---

## 3. クラスメンバ (ClassMember)

インスタンス変数やフィールドを定義します。

### 基本構文
```dsl
[+|-|#|~] [s|a]? <memberName> : <type>
```

*   **`[+|-|#|~]`**: アクセス修飾子（Visibility）。
    *   `+`: public
    *   `-`: private
    *   `#`: protected
    *   `~`: package (パッケージプライベート)
*   **`[s|a]?`**: 静的/抽象指定。
    *   `s`: static (`s`)
    *   `a`: abstract (`a`)
*   **`<memberName>`**: メンバ名（識別子）。
*   **`:`**: 区切り文字。
*   **`<type>`**: データ型。

---

## 4. クラス操作 (ClassOperation)

クラスに紐づくメソッドを定義します。

### 基本構文
```dsl
[+|-|#|~] [s|a]? <operationName>(<param1>:<type>, [<param2>:<type>], ...) [: <ReturnType>]
```

*   **基本要素**: 修飾子、静的/抽象指定、操作名、パラメータリスト。
*   **パラメータの記述**: `name: type` の形式で記述します。複数ある場合はカンマ区切りです。（例: `(userId:String, amount:Decimal)`)
*   **戻り値型 (Optional)**: コロン (`:`) の後に指定します。（例: `: Boolean`）。指定がない場合はデフォルト（通常は `void` に近いもの）と見なされます。

---

## 5. リレーションシップ (Relation/関連)

クラス間の構造的な関連性（UML表記に相当）を定義します。DSL上では、シンボルを用いて記述し、パース時に種類が判定されます。

### 基本構文
```dsl
<Source> <Symbol> <Target> [<Label>] [<Multiplicity>] [|<Multiplicity>]
```

**主要な記号と対応関係:**

| 記号 | 型 (Type) | 説明 |
| :--- | :--- | :--- |
| `->` | association (関連) | 一般的な関連。 |
| `o>` | aggregation (集約) | 全体と部分の親子関係（全体が消えても部分は生存可能）。 |
| `*>` | composition (合成) | 強力な親子関係（全体が消えると部分も破棄される）。 |
| `>|` | generalization (一般化/継承) | 継承関係。通常、クラス定義の `extends` や `implements` で処理されますが、図示上はこの記号を使います。 |
| `>/` | realization (具現化) | インターフェースの実装（関連）。 |
| `-/>` | dependency (依存) | 参照関係。 |

**例:**
```dsl
User ->> Order : "発行する" [1] [0..*] // UserからOrderへのアソシエーション
```

---

## 6. エンドポイント定義 (Endpoint Definition)

APIのルーティングエンドポイントを定義します。（Controller/Service層など）

**構文:**
```dsl
endpoint <METHOD> <Path>
```
*   `<METHOD>`: HTTPメソッド（`GET`, `POST`, `PUT`, `DELETE`, `PATCH`, `HEAD`, `OPTIONS`）。
*   `<Path>`: URIパス（例: `/api/v1/users`).

---

## 7. Gherkinシナリオ定義 (Behavior Specification)

システムが期待される振る舞いを、Gherkin形式で記述します。これは実行フローをモデル化し、バックリファレンスや制約抽出の主要なソースとなります。

### シナリオブロック
```dsl
Scenario: <シナリオ名> [src: Label | URL]
Given <前提条件となるステップテキスト>
When <アクションが発生するステップテキスト>
Then <期待される結果の検証ステップテキスト>
And ...
But ...
```

*   **`Scenario:`**: シナリオの開始。
    *   `[src: Label | URL]` は、外部ソースからの情報付与フィールドです。
*   **キーワード**: `Given`, `When`, `Then`, `And`, `But` などが使えます。これらはフロー上の順序を示します。
*   **Howブロック（特殊）**: 複雑な手順を記述し、実行時の処理順序ガイドとして機能します。
    ```dsl
    How:
        "最初のステップの具体的な指示。"
        "次のサブステップの指示。"
    ```

### Gherkinステップ内の付加情報抽出
ステップテキスト内からは以下の情報が構造化されます。

1.  **制約 (Constraint)**: 値や範囲に関する制約（例: 「受注金額は1000円以上であること」）。
2.  **状態推論 (State Inference)**: 要素が持つべき現在の状態（例: `未確認`, `処理中`）。
3.  **識別子参照**: クラスメンバ名や操作名がテキスト内で言及されている箇所（後述の正規化とバックリファレンスの基盤）。

---

## 8. 解析ロジックのハイライト (Parser Logic Summary)

| 機能 | 使用DSL要素/パターン | 目的 |
| :--- | :--- | :--- |
| **名前解決/正規化** | `alias` 宣言, テキスト内のキーワード | DSL中の日本語表記を、コード側の英語識別子に変換（例: 「受注金額」 $\to$ `orderAmount`）。 |
| **バックリファレンス** | Gherkinステップのテキスト内容 | ある識別子が、どのシナリオ、どのクラス/操作から参照されたかを記録し、網羅性を検証する。 |
| **提案生成 (Suggestion)** | クラス、操作、リレーション、backrefIndex | 構文的な欠落（例: パラメータ型未指定, 未定義の関連）や、利用頻度に基づく開発支援コマンドを提示する。 |

---