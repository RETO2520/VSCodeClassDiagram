# セルフチェックガイドライン (Dogfooding Guide)

このプロジェクトは、自身が提供する「クラス図エディタ」「DSL」「リバースエンジニアリング」機能を用いて、プロジェクト自身のアーキテクチャ品質を継続的にチェックします。

## 1. 目的
- **ドッグフーディング**: 開発者自身がツールを使用することで、バグの早期発見とユーザビリティの向上を図る。
- **アーキテクチャの整合性維持**: `vision.md` に定義された「Domain decisions, React renders」の原則が守られているかを視覚的・自動的に検証する。
- **技術的負債の可視化**: 肥大化したコンポーネントや複雑な依存関係を早期に特定する。

## 2. セルフチェック・フロー

### ステップ 1: リバースエンジニアリングによる現況把握
定期的に（または大規模なリファクタリング前後で）、プロジェクトのソースコードからクラス図を逆生成します。
1. コマンドパレットから `Generate Diagram from Source` を実行。
2. `src/domain` および `src/app` フォルダを対象に解析。
3. 生成された `diagram.json` を開き、以下の点を確認する：
   - ドメイン層（DomainModel）に UI ライブラリ（React等）への依存が混入していないか。
   - 循環参照が発生していないか。

### ステップ 2: Spec DSL による設計の定義
プロジェクトの重要なモジュールについては、`.diagram/*.spec` ファイルを手動で記述し、「あるべき姿」を定義します。
- `src/domain/models/DomainModel.ts` などのコアロジックを DSL で定義。
- リバースエンジニアリング結果と DSL 定義を比較し、乖離（実装漏れや不要な実装）をチェックする。

### ステップ 3: メトリクスとヒートマップの確認
実装済みのヒートマップ可視化機能（C-16）を利用します。
- `Class Metrics` を表示し、複雑度（Complexity）が高いクラスを特定する。
- 特に `status.md` で指摘されている `App.tsx` や `SpecDslParser.ts` のメトリクスを監視し、分割の優先順位を決定する。

### ステップ 4: バリデーションレポートの活用
`ValidationReport` 機能（C-21）を実行し、モデルの不整合をチェックします。
- 命名規則の違反。
- 継承関係の不備（抽象クラスが具体メソッドを持っていないか等）。
- リレーションシップの断絶。

## 3. 定点観測項目 (Key Performance Indicators)

以下の項目を `status.md` の更新に合わせてチェックしてください。

| カテゴリ | チェック内容 | 確認ツール |
| :--- | :--- | :--- |
| **アーキテクチャ** | App.tsx からロジックが ComponentService へ移行されているか | リバースエンジニアリング |
| **整合性** | DSL で定義した Command と実装された Command クラスが一致するか | Spec DSL エディタ |
| **品質** | 循環参照や巨大クラス（God Object）が存在しないか | ヒートマップ / クラス図 |
| **パターン** | デザインパターン（Factory, Singleton 等）が適切に適用されているか | RefactorSuggester |

## 4. CLI を用いた自動チェック（将来計画）

ビルドプロセスに CLI インタープリタ（C-10）を組み込み、以下の自動テストを実施予定です。
```bash
# 例: ドメインモデルのバリデーションを CLI で実行
npm run check-arch -- --input ./.diagram/core.spec --source ./src/domain
```

## 5. 改善のフィードバックループ

1. **検知**: ツールが設計上の問題を指摘する。
2. **記録**: `status.md` の「未実装 / 部分的な実装」セクションに課題を追加する。
3. **修正**: `Command` モデルに基づき、リファクタリングを実施する。
4. **検証**: 再度リバースエンジニアリングを行い、図面上で問題が解消されたことを確認する。

---
*このガイドライン自体も、プロジェクトの進化に合わせて更新されるべきものです。*